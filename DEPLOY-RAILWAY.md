# دیپلوی روی Railway

این پروژه برای دیپلوی روی [Railway](https://railway.app) آماده شده است.

## نکات مهم

- پنل برای مدیریت SoftEther روی **همان ماشین** طراحی شده، اما می‌تواند به یک SoftEther از راه دور هم متصل شود (از طریق تنظیمات اتصال).
- روی Railway، SoftEther خودش نصب نیست؛ فقط پنل مدیریت اجرا می‌شود. باید آدرس و پورت مدیریت SoftEther را بعد از ورود تنظیم کنید.
- داده‌ها (SQLite، کلیدها، گواهی‌ها) در volume پایدار ذخیره می‌شوند.

## مراحل

1. ریپوی خود را به Railway وصل کنید (New Project → Deploy from GitHub).
2. Railway به طور خودکار Dockerfile را تشخیص می‌دهد.
3. متغیرهای محیطی پیشنهادی:

```
SEM_DATA_DIR=/data
SEM_WEB_PATH=          # خالی = ریشه، یا یک مسیر مخفی مثل mypanel
SEM_SECRET_KEY=        # اختیاری، در اولین اجرا تولید می‌شود
PORT=8000              # Railway خودش ست می‌کند
```

4. یک Volume به مسیر `/data` متصل کنید تا دیتابیس و تنظیمات باقی بمانند.
5. بعد از دیپلوی، به آدرس سرویس بروید و حساب اول را بسازید.
6. در صفحه Connect، آدرس SoftEther (مثلاً IP:5555) و رمز ادمین را وارد کنید.

## ساخت محلی برای تست

```bash
# فرانت
cd app/web && npm install && npm run build

# بک‌اند
pip install -r requirements.txt
SEM_DATA_DIR=./data python run.py
```

## زبان فارسی

زبان پیش‌فرض فارسی است. از صفحه ورود یا تنظیمات می‌توانید بین فارسی و انگلیسی جابه‌جا شوید. رابط کاربری از راست به چپ (RTL) پشتیبانی می‌کند.
